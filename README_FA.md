# 🎬 سیستم خودکار زیرنویس فارسی
## Persian Auto Subtitle System - AI-Powered

یک نرم‌افزار کامل و حرفه‌ای برای تولید خودکار زیرنویس فارسی برای ویدیوها با استفاده از هوش مصنوعی

---

## ✨ ویژگی‌ها

- ✅ **تشخیص خودکار گفتار** با استفاده از مدل Whisper
- ✅ **ترجمه خودکار** از زبان‌های مختلف به فارسی
- ✅ **شخصی‌سازی کامل** فونت، رنگ و موقعیت زیرنویس
- ✅ **چسباندن زیرنویس روی ویدیو** (Hardcoding)
- ✅ **رابط گرافیکی ساده** و کاربرپسند
- ✅ **پشتیبانی کامل از GPU** برای سرعت بالا
- ✅ **کاملاً آفلاین** و بدون محدودیت
- ✅ **سازگار با ویندوز و لینوکس**

---

## 📋 پیش‌نیازها

### 1. نصب Python
برنامه نیازمند Python نسخه 3.8 یا بالاتر است.

**ویندوز:**
- از [python.org](https://www.python.org/downloads/) دانلود کنید
- حین نصب گزینه "Add Python to PATH" را فعال کنید

**لینوکس:**
```bash
sudo apt update
sudo apt install python3 python3-pip python3-tk

### 2. نصب FFmpeg
FFmpeg برای پردازش ویدیو ضروری است.

**ویندوز:**
1. از [ffmpeg.org](https://ffmpeg.org/download.html) دانلود کنید
2. فایل را استخراج کنید
3. پوشه `bin` را به PATH سیستم اضافه کنید

یا با استفاده از Chocolatey:
cmd
choco install ffmpeg

**لینوکس:**
bash
sudo apt install ffmpeg

یا:
bash
sudo dnf install ffmpeg  # Fedora
sudo pacman -S ffmpeg    # Arch

### 3. نصب CUDA (اختیاری - برای استفاده از GPU)
برای سرعت بخشیدن به پردازش، نصب CUDA Toolkit توصیه می‌شود.

- دانلود از [NVIDIA CUDA](https://developer.nvidia.com/cuda-downloads)
- فقط برای کارت‌های گرافیک NVIDIA

---

## 🚀 نصب و راه‌اندازی

### مرحله 1: دانلود فایل‌ها
فایل‌های `persian_subtitle_app.py` و `requirements.txt` را دانلود کنید.

### مرحله 2: نصب کتابخانه‌ها

**ویندوز:**
cmd
cd C:\path\to\downloaded\files
pip install -r requirements.txt

**لینوکس:**
bash
cd /path/to/downloaded/files
pip3 install -r requirements.txt

**نکته مهم:** نصب ممکن است چند دقیقه طول بکشد (بسته به سرعت اینترنت).

### مرحله 3: نصب فونت وزیرمتن (پیشنهادی)
برای نمایش بهتر زیرنویس‌های فارسی:

1. فونت Vazirmatn را از [GitHub](https://github.com/rastikerdar/vazirmatn) دانلود کنید
2. فایل `.ttf` را نصب کنید

---

## 🎯 نحوه استفاده

### اجرای برنامه

**ویندوز:**
cmd
python persian_subtitle_app.py

**لینوکس:**
bash
python3 persian_subtitle_app.py

### مراحل کار با برنامه

1. **انتخاب ویدیو**
   - روی دکمه "انتخاب فایل" کلیک کنید
   - فایل ویدیوی خود را انتخاب کنید

2. **انتخاب پوشه خروجی**
   - محلی برای ذخیره فایل‌های خروجی مشخص کنید

3. **تنظیم زبان**
   - زبان صوتی ویدیو را مشخص کنید
   - برای ویدیوهای غیرفارسی، ترجمه خودکار انجام می‌شود

4. **شخصی‌سازی (اختیاری)**
   - در تب "تنظیمات ظاهری":
- فونت دلخواه را انتخاب کنید
- رنگ متن و حاشیه را تنظیم کنید
- موقعیت زیرنویس را مشخص کنید

5. **تنظیمات پیشرفته (اختیاری)**
   - در تب "تنظیمات پیشرفته":
- اندازه مدل را بر اساس سرعت/دقت مورد نیاز انتخاب کنید

6. **شروع پردازش**
   - دکمه "▶️ شروع پردازش" را بزنید
   - منتظر بمانید تا پردازش تکمیل شود

---

## ⚙️ تنظیمات پیشنهادی

### برای سرعت بالا:
- **Model Size:** small یا medium
- **GPU:** فعال (در صورت وجود)

### برای دقت بالا:
- **Model Size:** large-v3
- **Beam Size:** 5 (پیش‌فرض)

### برای سیستم‌های ضعیف:
- **Model Size:** tiny یا base
- **GPU:** خاموش (استفاده از CPU)

---

## 🎨 شخصی‌سازی زیرنویس

### فونت‌های پیشنهادی فارسی:
- **Vazirmatn** (توصیه می‌شود) - مدرن و خوانا
- **Samim** - کلاسیک و رسمی
- **Shabnam** - گرد و دوستانه
- **Sahel** - ساده و واضح

### رنگ‌های محبوب:
- **سفید:** `#FFFFFF` (پیش‌فرض)
- **زرد:** `#FFFF00` (برای تاکید)
- **آبی روشن:** `#00FFFF`

### تنظیمات حاشیه:
- **ضخامت 2-3:** برای وضوح بهتر
- **رنگ سیاه:** `#000000` (پیش‌فرض)

---

## 📊 مصرف منابع

| اندازه مدل | RAM مورد نیاز | سرعت | دقت |
|------------|---------------|------|-----|
| tiny       | ~1 GB         | ⭐⭐⭐⭐⭐ | ⭐⭐ |
| base       | ~1 GB         | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| small      | ~2 GB         | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| medium     | ~5 GB         | ⭐⭐ | ⭐⭐⭐⭐ |
| large-v3   | ~10 GB        | ⭐ | ⭐⭐⭐⭐⭐ |

---

## ❓ حل مشکلات رایج

### خطا: "FFmpeg not found"
**راهکار:** FFmpeg را نصب کرده و به PATH اضافه کنید.

### خطا: "CUDA not available"
**راهکار:** 
- اگر GPU ندارید، برنامه خودکار از CPU استفاده می‌کند
- برای استفاده از GPU، CUDA Toolkit را نصب کنید

### پردازش خیلی کند است
**راهکار:**
- از مدل کوچک‌تر استفاده کنید (small یا base)
- GPU را فعال کنید
- ویدیو را به قطعات کوچک‌تر تقسیم کنید

### زیرنویس روی ویدیو نمایش داده نمی‌شود
**راهکار:**
- مطمئن شوید FFmpeg درست نصب شده
- فایل فونت را در کنار برنامه قرار دهید
- از فرمت ویدیوی استاندارد (MP4, MKV) استفاده کنید

### متن فارسی به درستی نمایش داده نمی‌شود
**راهکار:**
- فونت فارسی (مثل Vazirmatn) را نصب کنید
- از فرمت زیرنویس ASS استفاده کنید (پیش‌فرض برنامه)

### خطا در ترجمه
**راهکار:**
- مطمئن شوید اتصال اینترنت برای دانلود اولیه مدل فعال است
- حافظه کافی (حداقل 8GB RAM) داشته باشید

---

## 🔧 استفاده پیشرفته

### اجرا از خط فرمان (بدون GUI)
می‌توانید یک اسکریپت ساده ایجاد کنید:

python
from persian_subtitle_app import *

# کد سفارشی خود را اینجا بنویسید

### پردازش دسته‌ای چند ویدیو
می‌توانید یک حلقه برای پردازش چند فایل بنویسید:

python
import os
from pathlib import Path

video_folder = "path/to/videos"
for video in Path(video_folder).glob("*.mp4"):
# پردازش هر ویدیو
pass

---

## 📝 نکات مهم

1. **مدیریت حافظه:**
   - برای ویدیوهای طولانی (>1 ساعت) از مدل‌های کوچک‌تر استفاده کنید
   - برنامه‌های دیگر را ببندید

2. **کیفیت ویدیو:**
   - کیفیت صدای ویدیو تاثیر مستقیم بر دقت دارد
   - از ویدیوهایی با صدای واضح استفاده کنید

3. **امنیت:**
   - تمام پردازش‌ها آفلاین انجام می‌شود
   - هیچ داده‌ای به اینترنت ارسال نمی‌شود

4. **قانونی:**
   - فقط برای ویدیوهایی که حق استفاده از آن‌ها را دارید استفاده کنید

---

## 🆘 پشتیبانی

در صورت بروز مشکل:

1. ابتدا بخش "حل مشکلات رایج" را بررسی کنید
2. لاگ خطاها را در پنجره برنامه چک کنید
3. مطمئن شوید تمام پیش‌نیازها نصب شده‌اند

---

## 📜 مجوز و حق نشر

این برنامه با استفاده از کتابخانه‌های متن‌باز ایجاد شده است:
- **Faster Whisper** (OpenAI)
- **NLLB** (Meta/Facebook)
- **FFmpeg**
- **pysubs2**

---

## 🙏 تشکر

این پروژه با الهام از گزارش جامع فنی طراحی و پیاده‌سازی شده است.

**نکته:** این نرم‌افزار کاملاً رایگان و آزاد است. از آن لذت ببرید! 🎉


حالا یک اسکریپت نصب خودکار برای راحتی بیشتر ایجاد می‌کنم:
```/home/claude/install
@echo off
chcp 65001 >nul
echo ============================================
echo نصب خودکار سیستم زیرنویس فارسی
echo Persian Auto Subtitle - Installation
echo ============================================
echo.

echo [1/4] بررسی Python...
python --version >nul 2>&1
if %errorlevel% neq 0 (
    echo ❌ Python یافت نشد!
    echo لطفاً Python را از python.org نصب کنید.
    pause
    exit /b 1
)
echo ✅ Python نصب شده است

echo.
echo [2/4] بررسی FFmpeg...
ffmpeg -version >nul 2>&1
if %errorlevel% neq 0 (
    echo ⚠️ FFmpeg یافت نشد!
    echo توصیه می‌شود FFmpeg را از ffmpeg.org نصب کنید.
    echo برنامه بدون FFmpeg نمی‌تواند زیرنویس را به ویدیو بچسباند.
    pause
) else (
    echo ✅ FFmpeg نصب شده است
)

echo.
echo [3/4] نصب کتابخانه‌های Python...
echo این مرحله ممکن است چند دقیقه طول بکشد...
python -m pip install --upgrade pip
python -m pip install -r requirements.txt

if %errorlevel% neq 0 (
    echo ❌ خطا در نصب کتابخانه‌ها!
    pause
    exit /b 1
)
echo ✅ کتابخانه‌ها با موفقیت نصب شدند

echo.
echo [4/4] ایجاد میانبر...
echo Set oWS = WScript.CreateObject("WScript.Shell") > CreateShortcut.vbs
echo sLinkFile = "%USERPROFILE%\Desktop\PersianSubtitle.lnk" >> CreateShortcut.vbs
echo Set oLink = oWS.CreateShortcut(sLinkFile) >> CreateShortcut.vbs
echo oLink.TargetPath = "python.exe" >> CreateShortcut.vbs
echo oLink.Arguments = "%CD%\persian_subtitle_app.py" >> CreateShortcut.vbs
echo oLink.WorkingDirectory = "%CD%" >> CreateShortcut.vbs
echo oLink.Description = "Persian Auto Subtitle System" >> CreateShortcut.vbs
echo oLink.Save >> CreateShortcut.vbs
cscript CreateShortcut.vbs >nul
del CreateShortcut.vbs
echo ✅ میانبر روی دسکتاپ ایجاد شد

echo.
echo ============================================
echo ✅ نصب با موفقیت تکمیل شد!
echo ============================================
echo.
echo برای اجرای برنامه:
echo 1. روی میانبر "PersianSubtitle" روی دسکتاپ کلیک کنید
echo 2. یا دستور زیر را اجرا کنید:
echo    python persian_subtitle_app.py
echo.
pause