

```markdown
# 🎬 سیستم خودکار زیرنویس فارسی (Persian Auto Subtitle System)

**یک نرم‌افزار کامل و حرفه‌ای برای تولید خودکار زیرنویس فارسی برای ویدیوها با قدرت هوش مصنوعی**

---

## ✨ ویژگی‌ها

- ✅ **تشخیص خودکار گفتار** با استفاده از مدل قدرتمند Whisper
- ✅ **ترجمه خودکار** از زبان‌های مختلف به فارسی
- ✅ **شخصی‌سازی کامل** فونت، رنگ، حاشیه و موقعیت زیرنویس
- ✅ **چسباندن زیرنویس روی ویدیو** (Hardcoding)
- ✅ **رابط گرافیکی ساده (GUI)** و کاربرپسند
- ✅ **پشتیبانی از GPU** برای افزایش چشمگیر سرعت
- ✅ **کاملاً آفلاین** و بدون نیاز به اینترنت (پس از دانلود مدل‌ها)
- ✅ **سازگار با ویندوز و لینوکس**

---

## 📋 پیش‌نیازها

### 1. نصب Python
برنامه نیازمند Python نسخه **3.8** یا بالاتر است.

- **ویندوز:** از [python.org](https://www.python.org/downloads/) دانلود کنید (گزینه "Add to PATH" را بزنید).
- **لینوکس:**
  ```bash
  sudo apt update
  sudo apt install python3 python3-pip python3-tk

```

### 2. نصب FFmpeg

برای پردازش ویدیو و چسباندن زیرنویس ضروری است.

* **ویندوز:**
1. دانلود از [ffmpeg.org](https://ffmpeg.org/download.html)
2. استخراج فایل و اضافه کردن پوشه `bin` به Environment Variables (PATH).
*یا نصب با Chocolatey:*


```cmd
choco install ffmpeg

```


* **لینوکس:**
```bash
sudo apt install ffmpeg

```



### 3. نصب CUDA (اختیاری - برای سرعت بیشتر)

برای استفاده از کارت گرافیک NVIDIA:

* دانلود از [NVIDIA CUDA Toolkit](https://developer.nvidia.com/cuda-downloads).

---

## 🚀 نصب و راه‌اندازی

شما می‌توانید به دو روش دستی یا خودکار برنامه را نصب کنید.

### روش اول: نصب سریع (ویندوز) ⚡

یک فایل متنی با نام `install.bat` بسازید، کد زیر را در آن کپی کنید و اجرا نمایید:

```bat
@echo off
chcp 65001 >nul
echo ============================================
echo نصب خودکار سیستم زیرنویس فارسی
echo Persian Auto Subtitle - Installation
echo ============================================
echo.

echo [1/4] بررسی Python...
python --version >nul 2>&1
if %errorlevel% neq 0 (
    echo ❌ Python یافت نشد!
    echo لطفاً Python را از python.org نصب کنید.
    pause
    exit /b 1
)
echo ✅ Python نصب شده است.

echo.
echo [2/4] بررسی FFmpeg...
ffmpeg -version >nul 2>&1
if %errorlevel% neq 0 (
    echo ⚠️ FFmpeg یافت نشد!
    echo توصیه می‌شود FFmpeg را نصب کنید تا امکان چسباندن زیرنویس فراهم شود.
    pause
) else (
    echo ✅ FFmpeg نصب شده است.
)

echo.
echo [3/4] نصب کتابخانه‌های Python...
python -m pip install --upgrade pip
python -m pip install -r requirements.txt

if %errorlevel% neq 0 (
    echo ❌ خطا در نصب کتابخانه‌ها!
    pause
    exit /b 1
)

echo.
echo [4/4] ایجاد میانبر...
echo Set oWS = WScript.CreateObject("WScript.Shell") > CreateShortcut.vbs
echo sLinkFile = "%USERPROFILE%\Desktop\PersianSubtitle.lnk" >> CreateShortcut.vbs
echo Set oLink = oWS.CreateShortcut(sLinkFile) >> CreateShortcut.vbs
echo oLink.TargetPath = "python.exe" >> CreateShortcut.vbs
echo oLink.Arguments = "%CD%\persian_subtitle_app.py" >> CreateShortcut.vbs
echo oLink.WorkingDirectory = "%CD%" >> CreateShortcut.vbs
echo oLink.Save >> CreateShortcut.vbs
cscript CreateShortcut.vbs >nul
del CreateShortcut.vbs
echo ✅ میانبر روی دسکتاپ ایجاد شد.

echo.
echo ✅ نصب با موفقیت تکمیل شد!
pause

```

### روش دوم: نصب دستی

1. **دانلود فایل‌ها:** فایل‌های پروژه (`persian_subtitle_app.py` و `requirements.txt`) را دانلود کنید.
2. **نصب وابستگی‌ها:**
**ویندوز:**
```cmd
pip install -r requirements.txt

```


**لینوکس:**
```bash
pip3 install -r requirements.txt

```


3. **نصب فونت (پیشنهادی):**
برای نمایش صحیح و زیبای فارسی، فونت [Vazirmatn](https://github.com/rastikerdar/vazirmatn) را دانلود و نصب کنید.

---

## 🎯 نحوه استفاده

### اجرای برنامه

```bash
# ویندوز
python persian_subtitle_app.py

# لینوکس
python3 persian_subtitle_app.py

```

### راهنمای گام‌به‌گام

1. **انتخاب فایل:** ویدیو را انتخاب کنید.
2. **مسیر خروجی:** پوشه ذخیره فایل نهایی را مشخص کنید.
3. **زبان:** زبان اصلی ویدیو را انتخاب کنید (برای ویدیوهای خارجی، ترجمه خودکار انجام می‌شود).
4. **تنظیمات ظاهری:** فونت، رنگ و مکان زیرنویس را به دلخواه تغییر دهید.
5. **شروع:** دکمه "شروع پردازش" را بزنید.

---

## ⚙️ تنظیمات پیشنهادی (Configuration)

| سناریو | مدل (Model Size) | وضعیت GPU | Beam Size |
| --- | --- | --- | --- |
| **سرعت بالا** | small / medium | ✅ فعال | - |
| **دقت بالا** | large-v3 | ✅ فعال | 5 |
| **سیستم ضعیف** | tiny / base | ❌ خاموش | - |

---

## 📊 مقایسه مدل‌ها

| اندازه مدل | RAM مورد نیاز | سرعت | دقت |
| --- | --- | --- | --- |
| **tiny** | ~1 GB | ⭐⭐⭐⭐⭐ | ⭐⭐ |
| **base** | ~1 GB | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| **small** | ~2 GB | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| **medium** | ~5 GB | ⭐⭐ | ⭐⭐⭐⭐ |
| **large-v3** | ~10 GB | ⭐ | ⭐⭐⭐⭐⭐ |

---

## 🎨 راهنمای شخصی‌سازی زیرنویس

### فونت‌های پیشنهادی

* **Vazirmatn:** مدرن و استاندارد (توصیه شده)
* **Sahel:** ساده و خوانا
* **Lalezar:** مناسب برای تیترها یا ویدیوهای فان

### کدهای رنگی محبوب

* سفید: `#FFFFFF`
* زرد: `#FFFF00`
* سایان (آبی روشن): `#00FFFF`

---

## ❓ حل مشکلات رایج

<details>
<summary><b>خطای "FFmpeg not found"</b></summary>
FFmpeg نصب نشده یا در PATH سیستم نیست. طبق راهنمای پیش‌نیازها آن را نصب کنید.
</details>

<details>
<summary><b>خطای "CUDA not available"</b></summary>
برنامه کارت گرافیک NVIDIA را شناسایی نکرده است. درایور کارت گرافیک و CUDA Toolkit را نصب کنید یا از حالت CPU استفاده کنید.
</details>

<details>
<summary><b>زیرنویس فارسی مربع یا ناخوانا است</b></summary>
فونت مناسب نصب نشده است. فونت Vazirmatn را نصب کرده و در تنظیمات برنامه آن را انتخاب کنید.
</details>

---

## 🔧 استفاده پیشرفته (برنامه‌نویسان)

### اسکریپت‌نویسی با پایتون

```python
from persian_subtitle_app import *

# استفاده از توابع داخلی برای پردازش
# ...

```

### پردازش دسته‌ای (Batch Processing)

```python
import os
from pathlib import Path

video_folder = "path/to/videos"
for video in Path(video_folder).glob("*.mp4"):
    print(f"Processing {video}...")
    # اضافه کردن کد پردازش

```

---

## 📜 مجوز و تکنولوژی‌ها

این پروژه بر پایه کتابخانه‌های متن‌باز زیر توسعه یافته است:

* [Faster Whisper](https://github.com/guillaumekln/faster-whisper) (OpenAI)
* [NLLB](https://github.com/facebookresearch/fairseq/tree/nllb) (Meta AI)
* [FFmpeg](https://ffmpeg.org/)
* [pysubs2](https://github.com/tkarabela/pysubs2)

**توسعه و حق کپی‌رایت:** آزاد و رایگان (MIT License)

```

```
